<h1 class="w-[80%]">
  Are you interested in our business or do you have any questions?
</h1>
<form
  action="////"
  method="POST"
  class="flex w-full flex-col items-center justify-center gap-4"
>
  <input
    type="text"
    name="name"
    placeholder="Name"
    required
    autocomplete="off"
  />
  <input
    type="email"
    name="email"
    placeholder="Email"
    required
    autocomplete="off"
  />
  <input
    type="text"
    name="Affair"
    placeholder="Affair"
    required
    autocomplete="off"
  />
  <textarea name="comments" placeholder="Comment" required></textarea>
  <button type="submit">SEND</button>
</form>

<style>
  form > input {
    @apply w-72 rounded-lg border-[3px] border-Primary bg-Secondary py-2 pl-4 text-base text-White placeholder:font-normal placeholder:text-Primary;
  }
  form > textarea {
    @apply h-36 w-72 rounded-lg border-[3px] border-Primary bg-Secondary pl-4 pt-2 text-base text-White placeholder:font-normal placeholder:text-Primary;
  }
  form > button {
    @apply mb-4 flex h-8 w-14 items-center justify-center rounded-lg border-[3px] border-Primary bg-Secondary text-xs font-semibold text-Primary transition-all duration-300 ease-out hover:scale-105 hover:bg-Primary hover:text-Secondary xs:mb-6 xs:h-10 xs:w-36 xs:text-base sm:mb-8;
  }
</style>
